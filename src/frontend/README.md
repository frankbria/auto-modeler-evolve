# AutoModeler — Frontend

Next.js (App Router) frontend for AutoModeler. See the [root README](../../README.md)
for the project overview, stack, and setup.

```bash
npm install
npm run dev    # http://localhost:3000
npm test       # Jest
```
